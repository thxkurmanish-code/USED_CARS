# Features

Product capabilities will live here as self-contained modules. Each feature should own its UI, view-model helpers, and API-facing behavior while shared primitives remain in `components`, `lib`, and `services`.
